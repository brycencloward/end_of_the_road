import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wpgrubin.shuttle',
  appName: 'idaho-wildlife-shuttle-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
