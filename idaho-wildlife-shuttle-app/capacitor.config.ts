import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.LCstate.shuttle',
  appName: 'idaho-wildlife-shuttle-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
