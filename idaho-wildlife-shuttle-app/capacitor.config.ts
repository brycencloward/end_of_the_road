import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'idaho-wildlife-shuttle-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
